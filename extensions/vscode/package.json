{
  "name": "clike",
  "displayName": "Clike",
  "description": "From intent to impact. CLike keeps developers in flow, augments delivery with agentic workflows, and bakes in governance, eval‑driven quality, and a safe paved road for enterprises w/ an AI native pipeline.",
  "version": "0.5.3",
  "publisher": "authenticfake",
  "icon": "images/clike_64x64.png",
  "engines": {
    "vscode": "^1.85.0"
  },
  "author": {
    "name": "authenticfake"
  },
  "main": "./extension.js",
  "activationEvents": [
    "onStartupFinished"
  ],
  "categories": [
    "AI",
    "Programming Languages"
  ],
   "repository": {
    "type": "git",
    "url": "https://github.com/authenticfake/clike.git"
  },
  "homepage": "https://github.com/authenticfake/clike/tree/main",
  "contributes": {
    "commands": [
      {
        "command": "clike.openChat",
        "title": "CLike: Chat (Q&A / Harper / Coding)"
      },
      { "command": "clike.harper.init", "title": "CLike: Harper Init…" },
      {
        "command": "clike.chat.clearSession",
        "title": "CLike: Clear Chat Session (current model)"
      },
      {
        "command": "clike.chat.openSessionFile",
        "title": "CLike: Chat Session File (current model)"
      },
      {
        "command": "clike.ping",
        "title": "Clike: Ping Service"
      },
      {
        "command": "clike.codeAction",
        "title": "Clike: Code Action\u2026"
      },
      {
        "command": "clike.addDocstring",
        "title": "Clike: Add Docstring (AI via Orchestrator)"
      },
      {
        "command": "clike.refactor",
        "title": "Clike: Refactor (AI via Orchestrator)"
      },
      {
        "command": "clike.generateTests",
        "title": "Clike: Generate Tests (AI via Orchestrator)"
      },
      {
        "command": "clike.fixErrors",
        "title": "Clike: Fix Errors (AI via Orchestrator)"
      },
      {
        "command": "clike.applyUnifiedDiffHardened",
        "title": "Clike: Apply Unified Diff (Hardened)"
      },
      {
        "command": "clike.applyUnifiedDiff",
        "title": "Clike: Apply Unified Diff"
      },
      {
        "command": "clike.applyNewContent",
        "title": "Clike: Apply New Content"
      },
      {
        "command": "clike.applyLastPatch",
        "title": "Clike: Apply Last Patch"
      },
      {
        "command": "clike.listModels",
        "title": "Clike: List Models (via Gateway)"
      },
      {
        "command": "clike.checkServices",
        "title": "Clike: Check Services"
      },
      {
        "command": "clike.ragReindex",
        "title": "Clike: RAG Reindex (via Orchestrator)"
      },
      {
        "command": "clike.ragSearch",
        "title": "Clike: RAG Search (via Orchestrator)"
      },
      {
        "command": "clike.gitCreateBranch",
        "title": "Clike: Git Create Branch"
      },
      {
        "command": "clike.gitCommitPatch",
        "title": "Clike: Git Commit Patch"
      },
      {
        "command": "clike.gitOpenPR",
        "title": "Clike: Git Open PR"
      },
      {
        "command": "clike.gitSmartPR",
        "title": "Clike: Git Smart PR"
      },
      { "command": "clike.eval.runAll", "title": "CLike: Run All Evals" },
      { "command": "clike.gate.checkPhase", "title": "CLike: Gate Check (Phase)" },
      { "command": "clike.constraints.sync", "title": "CLike: Sync Constraints (IDEA/SPEC)" },
      { "command": "clike.plan.updateChecklist", "title": "CLike: Update PLAN Checklist from Eval" },
      { "command": "clike.promoteReqSources", "title": "CLike: Promote source code" },
      { "command": "clike.promoteReqSourcesQuick", "title": "CLike: Quick Promote source code" }
    ],
    "configuration": {
      "title": "Clike",
      "properties": {
        "clike.chat.theme": {
          "type": "string",
          "default": "pro",
          "enum": ["classic", "pro", "studio", "paper"],
          "enumDescriptions": [
            "Classic CLike dark (storico, ping-pong)",
            "Pro doc-style (colonna unica, neutro, allineato a VS Code)",
            "Studio dark console (più denso, focus contenuti)",
            "Paper light (notebook chiaro)"
          ], 
          "description": "Theme for the Chat CLike UI."
        },
        "clike.harperTimeout": {
          "type": "number",
          "default": 25,
          "description": "timeout in minutes for harper phase completions."
        },
        "clike.verboseLogging": {
          "type": "boolean",
          "default": false,
          "description": "Log verbose messages."
        },
        "clike.docRoot": {
          "type": "string",
          "default": "docs/harper",
          "description": "Doc root for harper scenario(relativa alla workspace)."
        },
         "publisher.clike": {
          "type": "boolean",
          "default": "${publisher}.${name}",
          "description": "Log verbose messages."
        },
        "clike.chat.persistDir": {
          "type": "string",
          "default": ".clike/sessions",
          "description": "Directory (relativa alla workspace) dove salvare le chat per modello (formato JSONL)."
        },
         "clike.chat.autoOpenOnStartup": {
          "type": "boolean",
          "default": true,
          "description": "Auto-open chat at startup."
        },
        "clike.chat.never_send_source_to_cloud": {
          "type": "boolean",
          "default": true,
          "description": "Never send code to Cloud"
        },
        "clike.chat.maxInMemoryMessages": {
          "type": "number",
          "default": 50,
          "description": "Numero massimo di messaggi tenuti in memoria nella webview per non sforare RAM."
        },
        "clike.chat.autoWriteGeneratedFiles": {
          "type": "boolean",
          "default": true,
          "description": "Se true, i file generati in mode=coding vengono scritti subito su FS."
        },
        "clike.orchestratorUrl": {
          "type": "string",
          "default": "http://localhost:8080",
          "description": "Base URL for the Clike orchestrator API (e.g. http://localhost:8080)."
        },
        "clike.gatewayUrl": {
          "type": "string",
          "default": "http://localhost:8000",
          "description": "Base URL for the Clike gateway API (e.g. http://localhost:8000)."
        },
        "clike.optimizeFor": {
          "type": "string",
          "default": "capability",
          "enum": [
            "latency",
            "cost",
            "capability"
          ],
          "description": "Optimization mode sent to the orchestrator."
        },
        "clike.execution.defaultPreference": {
          "type": "string",
          "default": "local_agent_only",
          "enum": [
            "cloud_only",
            "prefer_local_agent",
            "local_agent_only"
          ],
          "description": "Default execution preference used by CLike for compatible tasks. Legacy values are normalized at runtime: auto -> cloud_only, hybrid -> prefer_local_agent, prefer_claude_code -> prefer_local_agent, claude_code_only -> local_agent_only."
        },
        "clike.localAgent.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable local agent execution for compatible Harper flows."
        },
        "clike.localAgent.preferredExecutor": {
          "type": "string",
          "default": "gpt_codex",
          "enum": [
            "auto",
            "claude_code",
            "gpt_codex"
          ],
          "description": "Preferred local agent executor when execution preference selects the local path."
        },
        "clike.localAgent.allowEval": {
          "type": "boolean",
          "default": true,
          "description": "Enable local agent pre-pass for /eval before the canonical CLike eval path."
        },
        "clike.localAgent.restrictToKitPhases": {
          "type": "boolean",
          "default": true,
          "description": "Restrict local agent usage to base /kit flows and disable local follow-up phases for now."
        },
        "clike.localAgent.timeoutMinutes": {
          "type": "number",
          "default": 30,
          "minimum": 1,
          "description": "Default timeout in minutes for local agent runs triggered by CLike."
        },
        "clike.execution.showInChat": {
          "type": "boolean",
          "default": true,
          "description": "Show execution path details in the CLike chat UI."
        },
        "clike.claudeCode.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable Claude Code as a local executor when its CLI is installed. Set to false to disable Claude Code locally."
        },
        "clike.claudeCode.restrictToKitPhases": {
          "type": "boolean",
          "default": true,
          "description": "Restrict Claude Code usage to compatible /kit flows."
        },
        "clike.claudeCode.command": {
          "type": "string",
          "default": "claude",
          "description": "Executable used to invoke Claude Code locally."
        },
        "clike.claudeCode.model": {
          "type": "string",
          "default": "opus",
          "description": "Model passed to Claude Code on every local run (--model). Use a tier alias like 'opus' or 'sonnet' to always get the latest model of that tier, or a full model id to pin a version. Leave empty to use the Claude CLI default."
        },
        "clike.claudeCode.printModeFlag": {
          "type": "string",
          "default": "-p",
          "description": "Flag used for non-interactive Claude Code execution (for example -p or --print depending on your local version)."
        },
        "clike.claudeCode.timeoutMinutes": {
          "type": "number",
          "default": 30,
          "minimum": 1,
          "description": "Timeout in minutes for synchronous Claude Code runs triggered by CLike."
        },
        "clike.claudeCode.enableEval": {
          "type": "boolean",
          "default": false,
          "description": "Reserved for the next tranche. Keep false for now: /eval remains owned by CLike."
        },
        "clike.claudeCode.permissionMode": {
          "type": "string",
          "default": "acceptEdits",
          "description": "Claude Code permission mode used for local non-interactive execution."
        },
        "clike.localAgent.codex.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable GPT Codex local executor integration when available locally."
        },
        "clike.localAgent.codex.command": {
          "type": "string",
          "default": "codex",
          "description": "Executable used to invoke GPT Codex locally."
        },
        "clike.localAgent.codex.model": {
          "type": "string",
          "default": "gpt-5.5",
          "description": "Model passed to Codex on every local run (--model). Codex exposes no model-list command, so set the exact model id your Codex login supports; an invalid id makes the run fail. Leave empty to use the Codex CLI default."
        },
        "clike.localAgent.codex.approvalMode": {
          "type": "string",
          "default": "",
          "description": "Reserved. Do not use for now: Codex CLI options vary by version and unsupported flags break execution."
        },
        "clike.localAgent.codex.printModeFlag": {
          "type": "string",
          "default": "",
          "description": "Reserved. Codex non-interactive execution is currently invoked as: codex exec <prompt>."
        },
        "clike.localAgent.codex.sandboxMode": {
          "type": "string",
          "default": "auto",
          "enum": [
            "auto",
            "read-only",
            "workspace-write",
            "danger-full-access"
          ],
          "description": "Codex CLI sandbox mode. auto uses workspace-write for write-required local-agent phases and read-only for inspect-only phases."
        },
        "clike.localAgent.codex.timeoutMinutes": {
          "type": "number",
          "default": 30,
          "minimum": 1,
          "description": "Timeout in minutes for GPT Codex runs triggered by CLike."
        },
        "clike.mcp.clientEnabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable MCP client integrations inside CLike."
        },
        "clike.mcp.serverEnabled": {
          "type": "boolean",
          "default": true,
          "description": "Expose the read-only/service CLike MCP server from the orchestrator."
        },
        "clike.mcp.extensionServerEnabled": {
          "type": "boolean",
          "default": true,
          "description": "Expose the local CLike operational MCP-compatible server from the VS Code extension."
        },
        "clike.mcp.extensionServerHost": {
          "type": "string",
          "default": "127.0.0.1",
          "description": "Host for the local CLike extension operational MCP-compatible server."
        },
        "clike.mcp.extensionServerPort": {
          "type": "number",
          "default": 55742,
          "minimum": 1024,
          "description": "Port for the local CLike extension operational MCP-compatible server."
        },
        "clike.mcp.extensionServerToken": {
          "type": "string",
          "default": "",
          "description": "Optional bearer token required by the local CLike extension operational MCP-compatible server. Empty means localhost-only without token."
        },
        "clike.apply.backup": {
          "type": "boolean",
          "default": true,
          "description": "If true, create a ~clike.bak backup before applying patches."
        },
        "clike.apply.requireCleanGit": {
          "type": "boolean",
          "default": false,
          "description": "If true, require a clean Git working tree before applying patches."
        },
        "clike.apply.dryRunPreview": {
          "type": "boolean",
          "default": false,
          "description": "If true, run a dry-run and show preview before applying the patch."
        },
        "clike.git.autoCommit": {
          "type": "boolean",
          "default": true,
          "description": "If true, auto-commit changes after apply."
        },
        "clike.git.gitMergeOnGate": {
          "type": "boolean",
          "default": true,
          "description": "If true, merge changes on gate."
        },

        "clike.git.gitDeleteBranchOnMerge": {
          "type": "boolean",
          "default": false,
          "description": "If true, delete branch on merge."
        },

         "clike.git.gitReturnToFeatureAfterMerge": {
          "type": "boolean",
          "default": false,
          "description": "If true, return to feature branch after merge."
        },

        "clike.git.remoteUrl": {
          "type": "string",
          "default": "",
          "description": "Optional remote URL to set (e.g., git@github.com:org/repo.git). If empty, Clike will not add/set the remote automatically."
        },

        "clike.git.commitMessage": {
          "type": "string",
          "default": "clike: apply patch (AI)",
          "description": "Default commit message used for auto-commit."
        },
        "clike.git.openPR": {
          "type": "boolean",
          "default": true,
          "description": "If true, open PR flow after auto-commit (requires GitHub extension)."
        },
        "clike.git.remote": {
          "type": "string",
          "default": "origin",
          "description": "Default remote name for PR flow."
        },
         "clike.git.defaultBranch": {
          "type": "string",
          "default": "main",
          "description": "Default branch for PR flow."
        },
        "clike.git.conventionalCommits": {
          "type": "boolean",
          "default": true,
          "description": "If true, use conventional commits for PR flow."
        },
        "clike.git.pushRebase": {
          "type": "boolean",
          "default": true,
          "description": "If true, push changes after PR flow."
        },
        "clike.git.branchPrefix": {
          "type": "string",
          "default": "feature",
          "description": "Prefix for feature branch names."
        },
        "clike.git.tagPrefix": {
          "type": "string",
          "default": "harper",
          "description": "Prefix for tag names."
        },
        "clike.git.prPerReqDraft.enabled": {
          "type": "boolean",
          "default": false,
          "description": "If true, create a PR per request draft."
        },
        "clike.git.prPerReqDraft.useGhCli": {
          "type": "boolean",
          "default": true,
          "description": "If true, use GitHub CLI to create PRs."
        },
         "clike.git.prBodyPath": {
          "type": "string",
          "default": "docs/harper/PR_BODY.md",
          "description": "Prefix for tag names."
        },
        "clike.routes": {
          "type": "object",
          "default": {
            "orchestrator": {
              "code": "/agent/code",
              "ragSearch": "/v1/rag/search",
              "ragReindex": "/v1/rag/reindex",
              "ragIndex": "/v1/rag/index",
              "health": "/health",
              "chat": "/v1/chat",
              "generate": "/v1/generate"

            },
            "gateway": {
              "models": "/v1/models",
              "chatCompletions": "/v1/chat/completions",
              "health": "/health"
            }
          },
          "description": "Override default relative routes for orchestrator and gateway."
        }
      }
    }
  },

  "dependencies": {
    "diff": "^5.2.0"
  }
}
